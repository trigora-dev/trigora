{
  "name": "@trigora/web",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "lint": "eslint .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  }
}
