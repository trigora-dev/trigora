import { defineFlow } from '@trigora/sdk';

// This flow runs on incoming webhook events
export default defineFlow({
  id: 'hello',
  trigger: { type: 'webhook' },
  async run(event, ctx) {
    await ctx.log.info('Webhook received', {
      payload: event.payload,
    });
    return 'Received webhook event';
  },
});